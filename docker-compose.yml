version: '3'
services:
        web:
                image: cafendback
                build: .
                ports:
                        - "8253:8253"
                volumes:
                        - ./volumes:/volumes
                        - ./instance:/instance
                restart: unless-stopped
